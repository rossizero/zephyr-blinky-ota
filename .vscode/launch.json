{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Zephyr Debug (Cortex-Debug)",
            "cwd": "${workspaceFolder}",
            "executable": "${workspaceFolder}/zephyr-project/build/zephyr/zephyr.elf",
            "request": "launch",
            "type": "cortex-debug",
            "servertype": "jlink",
            "device": "ATSAMD21G18",
            "interface": "swd"
        }
    ]
}